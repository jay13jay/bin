SELECT sys_users.login, accounts.password, sys_users.home, sys_users.shell FROM domains RIGHT JOIN hosting ON domains.id = hosting.dom_id LEFT JOIN sys_users ON hosting.sys_user_id = sys_users.id LEFT JOIN accounts ON sys_users.account_id = accounts.id WHERE domains.name = 'kurandatours.com.au';
